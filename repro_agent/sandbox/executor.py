"""Executor placeholder for milestone 2."""

from __future__ import annotations


class SandboxExecutor:
    def explain(self) -> str:
        return (
            "Sandbox execution will run repository commands inside Docker with "
            "resource limits and explicit approval gates."
        )
